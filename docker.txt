- Récupérer le digest du tag latest : https://hub.docker.com/v2/namespaces/library/repositories/nginx/tags/latest
- Liste des tags d'une image : https://hub.docker.com/v2/namespaces/library/repositories/nginx/tags?page_size=100
- Pour chaque tag :
	- check si le digest correspond au latest
	- Si oui, ajouter les infos dans une variable
	- Si non, continue

- Afficher le digest global (pas par archi)
- Afficher pour chaque info :
	- tag
	- tag_last_pushed (mettre le nombre de jour depuis aujourd'hui)
